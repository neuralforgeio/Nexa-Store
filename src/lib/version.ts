export const APP_NAME = "Nexa Store";

/** Single source of app version (A14) — shown in footer, about, and diagnostics. */
export const APP_VERSION = process.env.NEXT_PUBLIC_APP_VERSION?.trim() || "1.8.0";
